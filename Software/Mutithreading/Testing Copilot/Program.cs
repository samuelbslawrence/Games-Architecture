
class Program
{
    // This is a simple program that creates a new instance of TestThreads and calls the Start method.
    static void Main(string[] args)
    {
        Scene scene = new Scene();
        scene.Start();
    }
}